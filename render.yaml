services:
  - type: web
    name: carpool-backend
    runtime: node
    plan: free

    buildCommand: |
      npm install
      npm run prisma:generate
      npm run build

    startCommand: |
      npm run prisma:migrate
      npm run start

    envVars:
      - key: NODE_VERSION
        value: 22
